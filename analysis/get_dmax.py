import os
import sys

if len(sys.argv)!=1:
	print "Give file name"
	sys.exit()

f=open(sys.argv[1],'r')

print *,r,rho(iter),p(iter),p(iter)/rho(iter)/scl%rgas*muc,x(iter),y(iter),z(iter)
iter=0
for line in f:
	line=

os.system('rm -f tmpfile')
